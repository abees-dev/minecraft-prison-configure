# Agent Rules

- **Do Not Kill the Starting Server**: Never terminate, cancel, or kill the Minecraft server process (running via `run.bat` or `java`) while it is starting up or active.
- **Do Not Run the Start Script (`run.bat`) If Already Running**: Do not execute `run.bat` or launch the server if a server process is already starting or running. Check running tasks first before attempting to start the server.
